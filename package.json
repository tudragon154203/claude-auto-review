{
  "name": "claude-auto-review",
  "version": "1.0.0",
  "description": "Claude Code plugin that reviews changed code before Claude stops.",
  "type": "commonjs",
  "bin": {
    "claude-auto-review": "scripts/review-prompt.js"
  },
  "scripts": {
    "test": "vitest run",
    "setup": "node scripts/setup-claude-auto-review.js",
    "review": "node scripts/review-prompt.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  },
  "license": "MIT"
}
